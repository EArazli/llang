import "../../../stdlib/data/list.llang"

doctrine ListWithA extends ListData where {
  gen A : [] -> [U_M] @M;
}

pipeline main where {
  normalize;
  extract diagram;
}

run main using main where {
  source doctrine ListWithA;
  source mode M;
}
---
Nil(A)
---
