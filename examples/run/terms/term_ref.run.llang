import "../../lib/terms/term_defs.llang"

doctrine Ext extends Base where {
  gen g : [A] -> [A] @M;
}

pipeline main where {
  normalize;
  extract diagram;
}

run main using main where {
  source doctrine Ext;
}
---
@t ; g
---
