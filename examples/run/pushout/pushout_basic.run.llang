import "../../lib/pushout/pushout_basic.llang";

pipeline legacy_main where {
  normalize;
  extract diagram;
}

run main using legacy_main where {
  source doctrine Both;
  source mode M;
}
---
Left_inl_g ; Right_inr_g
---
