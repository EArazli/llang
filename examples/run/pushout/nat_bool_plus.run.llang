import "../../lib/pushout/nat_bool_plus.llang";

pipeline debug where {
  normalize { policy = "topmost"; fuel = 50; };
  extract diagram;
}

run main using debug where {
  source doctrine NatBoolPlus;
  source mode M;
}
---
Nat_inl_Z ; Nat_inl_S ; isZero
---
