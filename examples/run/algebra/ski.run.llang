import "../../lib/algebra/ski.llang";

pipeline main where {
  normalize;
  extract diagram;
}

run main using main where {
  source doctrine SKI;
  source mode M;
}
---
(((((s * k) ; app) * k) ; app) * id[T]) ; app
---
