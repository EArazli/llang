import "../../lib/algebra/ski.llang";

pipeline legacy_main where {
  normalize;
  extract diagram;
}

run main using legacy_main where {
  source doctrine SKI;
  source mode M;
}
---
(((((s * k) ; app) * k) ; app) * id[T]) ; app
---
