import "../../lib/templates/state.template.llang"

pipeline legacy_main where {
  normalize;
  extract diagram;
}

run main using legacy_main where {
  source doctrine StateNat;
  source mode M;
}
---
get
---
