import "../../lib/surfaces/template_splice_demo.surface.llang";

pipeline legacy_main where {
  normalize;
  extract diagram;
}

run main using legacy_main where {
  source doctrine SpliceBase;
  source mode M;
  source surface SpliceSurface;
}
---
term{}
---
