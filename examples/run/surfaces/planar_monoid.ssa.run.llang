import "../../lib/surfaces/planar_monoid.ssa.surface.llang";

pipeline debug where {
  normalize { policy = "topmost"; fuel = 50; };
  extract diagram;
}

run main using debug where {
  source doctrine CartMonoid;
  source mode M;
  source surface PlanarSSA;
}
---
diag {
  in x:A;
  in y:A;
  out mul(x, y);
}
---
