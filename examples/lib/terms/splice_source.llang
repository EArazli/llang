doctrine SpliceBase where {
  mode M classifiedBy M via M.U_M;
  type U_M @M;
  type A @M;
  gen f : [A] -> [A] @M;
  gen g : [A] -> [A] @M;
}

term SomePredefinedTerm where {
  doctrine SpliceBase;
  mode M;
}
---
f ; g
