doctrine BoxMatch where {
  mode M classifiedBy M via M.U_M;
  type U_M @M;
  type A @M;

  gen f : [A] -> [A] @M;
  gen g : [A] -> [A] @M;

  rule computational box_rule -> : [A] -> [A] @M =
    box B { f } == id[A]
}
