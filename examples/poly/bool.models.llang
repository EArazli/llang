import "../../stdlib/poly.bool.llang";

polymodel BoolNative : Bool where {
  default = symbolic;
  op dup(x) = [x, x];
  op drop(x) = [];
  op swap(x, y) = [y, x];
  op T() = true;
  op F() = false;
  op if(c,t,e) = if c then t else e;
}
