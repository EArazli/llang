import "./ccc.poly.llang";

polymodel CCCSymbolic : CCC where {
  default = symbolic;

  op dup(x) = [x, x];
  op drop(x) = [];
  op swap(x, y) = [y, x];
}

polyrun_spec STLCinCCC where {
  doctrine CCC;
  surface STLCSurface;
  model CCCSymbolic;
  policy UseOnlyComputationalLR;
  fuel 200;
  show normalized;
  show cat;
  show value;
}
