import "./struct.planar.llang";

polydoctrine StructSymmetric extends StructPlanar where {
  gen swap a b : [a, b] -> [b, a] @M;

  rule structural swap_involutive = a b : [a, b] -> [a, b] @M =
    swap ; swap == id[a, b]
}
