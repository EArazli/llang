doctrine ExceptionSchema where {
  mode M classifiedBy M via M.U_M;
  gen U_M : [] -> [M.U_M] @M;
  gen E : [] -> [M.U_M] @M;
}

doctrine_functor Exception(L : ExceptionSchema) where {
  gen raise : [L::M.L::E] -> [] @L::M;
}
