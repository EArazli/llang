doctrine WriterSchema where {
  mode M classifiedBy M via M.U_M;
  gen U_M : [] -> [M.U_M] @M;
  gen W : [] -> [M.U_M] @M;
}

doctrine_functor Writer(L : WriterSchema) where {
  gen tell : [L::M.L::W] -> [] @L::M;
}
